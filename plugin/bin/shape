#!/usr/bin/env bash
# Bare-name entry for the Bash tool PATH; the CLI itself is plain ESM in
# shape.mjs (bash cannot exec .mjs by name, node cannot shebang extensionless
# ESM, hence this two-line shim). Requires only node on PATH.
exec node "$(dirname "${BASH_SOURCE[0]}")/shape.mjs" "$@"
